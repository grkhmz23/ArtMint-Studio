import DashboardClient from "./DashboardClient";

export const metadata = {
  title: "Dashboard — ArtMint Studio",
  description: "Your ArtMint Studio dashboard",
};

export default function DashboardPage() {
  return <DashboardClient />;
}
